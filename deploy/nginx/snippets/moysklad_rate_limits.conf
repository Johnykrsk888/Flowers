# Подключать внутри location /api/moysklad/...
# ~20 запросов/с на IP + всплеск до 80 (каталог + много картинок); до 30 параллельных соединений на IP.
limit_req zone=moysklad_api burst=80 nodelay;
limit_conn moysklad_conn 30;
